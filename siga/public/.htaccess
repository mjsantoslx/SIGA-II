RewriteEngine On

# Serve ficheiros e pastas existentes directamente (CSS, JS, imagens).
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Tudo o resto passa pelo front controller.
RewriteRule ^ index.php [L]
